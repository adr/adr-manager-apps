packages:
  - "packages/*"

# pnpm 10 disables dependency build scripts by default — allow the native/postinstall ones these
# toolchains need. If install reports others, run `pnpm approve-builds` and add them here.
onlyBuiltDependencies:
  - esbuild
  - cypress
  - core-js
